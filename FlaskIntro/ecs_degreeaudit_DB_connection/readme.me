mysqlConnectionTest.py tests the connection to the ecs_degreeaudit database.
createTable.py creates tables to ecs_degreeaudit database.
addClassReq.py add/update course's pre/co-requisites.

All three files require user to provide user's NETID and password.
